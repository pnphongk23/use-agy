interface:
  display_name: "Use AGY"
  short_description: "Stream one AGY with bounded subagents"
  default_prompt: "Use $use-agy to stream one foreground primary AGY live, allow one useful read-only subagent, validate its structured handoff, and independently verify the result."
