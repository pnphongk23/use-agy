interface:
  display_name: "Use AGY"
  short_description: "Plan and supervise AGY execution workers"
  default_prompt: "Use $use-agy to plan this task, choose the right AGY execution topology, supervise the worker, and independently verify the result."
