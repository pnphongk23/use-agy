interface:
  display_name: "Use AGY"
  short_description: "Supervise AGY via handoff + ordered stream report"
  default_prompt: "Use $use-agy to write one handoff.md, run one foreground primary AGY via the run-agy.py watchdog runner, summarize the ordered timeline into report.md, allow one useful read-only subagent, review carefully, and resume at most once for a large corrective fix."
